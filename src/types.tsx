// types.ts or types.d.ts

export interface UserType {
    id: string;
    email: string;
    uid: string;
    photoURL: string;
    displayName: string
    // Add any other properties related to the user
  }
  